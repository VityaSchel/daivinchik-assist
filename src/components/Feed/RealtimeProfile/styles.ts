import { StyleSheet } from 'react-native'

export default StyleSheet.create({ 
  pending: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height: 200,

    title: {
      marginBottom: 10
    }
  }
})